// I intentionally omit the default export here - this file is kept as a placeholder
// after moving the page into app/dashboard/(protected)/page.tsx so the sidebar wraps it.
// Next.js App Router only registers a page when a default export is present.
export {}
