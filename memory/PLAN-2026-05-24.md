# Session Plan - 2026-05-24

Full implementation plan for the dashboard and CV overhaul session started on 2026-05-24.
Update the "Status" column as work progresses so a new chat session knows exactly where we are.

---

## Manual steps Isaac must run FIRST (before any dashboard code is deployed)

### Supabase SQL - run in SQL Editor
```sql
-- Add hidden column to notes (pinned and locked already exist)
ALTER TABLE notes ADD COLUMN IF NOT EXISTS hidden boolean DEFAULT false;

-- Add all three columns to diary
ALTER TABLE diary
  ADD COLUMN IF NOT EXISTS hidden  boolean DEFAULT false,
  ADD COLUMN IF NOT EXISTS pinned  boolean DEFAULT false,
  ADD COLUMN IF NOT EXISTS locked  boolean DEFAULT false;

-- Unique index on applications.url to prevent duplicate scraper inserts
CREATE UNIQUE INDEX IF NOT EXISTS applications_url_unique
  ON applications (url)
  WHERE url IS NOT NULL AND url <> '';

-- Fix LinkedIn slug (was "isaac-adjei", correct is "isaacadjei")
UPDATE config
SET value = jsonb_set(value, '{linkedin}', '"isaacadjei"')
WHERE key = 'me_profile';
```

### After Group A deploys
1. Go to https://isaacadjei.me/dashboard/me
2. Click the LinkedIn field (shows "isaac-adjei")
3. Change to `isaacadjei` (no hyphen) and save

### GITHUB_PAT check
GitHub -> Settings -> Developer settings -> Personal access tokens -> confirm `workflow` scope is ticked.

---

## Work groups and status

| Group | What | Status |
|-------|------|--------|
| A | Dashboard quick fixes (title, LinkedIn URL, sidebar on home, Course/Modules PIN, dark mode in Settings, test digest button, scraper error text) | PENDING |
| B | Login page redesign (avatar, name, styled card) | PENDING |
| C | CV content edits (profile, Aston bullets merge, award pipe format, Java + Kubernetes + PHP fix, synonyms, experience bullets merged with tech focus) | PENDING |
| D | CV Word download (new API route, button in CVViewer, extend cv-pdf.yml workflow) | PENDING |
| E | Applications page + scraper (rename tab, keyword fix, upsert dedup, more sources) | PENDING |
| F | Notes/Diary/Vault 3-dot menus + Now sub-section in Notes | PENDING |
| G | CV templates (6 role CVs + picker page + extend API routes) | PENDING (after C) |
| SQL | Update supabase-setup.sql with new column definitions | PENDING |

---

## Group A detail - Dashboard quick fixes

### A1: Tab title
File: `app/dashboard/(protected)/layout.tsx`
Add:
```ts
export const metadata = {
  title: { default: "Isaac Adjei | Dashboard", template: "%s | Isaac Adjei" },
  robots: "noindex, nofollow",
}
```

### A2: LinkedIn URL template
File: `app/dashboard/(protected)/me/MeClient.tsx` line 158
`https://linkedin.com/in/${profile.linkedin}` -> `https://www.linkedin.com/in/${profile.linkedin}/`

### A3: Dashboard home inside protected layout
Move `app/dashboard/page.tsx` -> `app/dashboard/(protected)/page.tsx` (delete old file).
Move `app/dashboard/DashboardHome.tsx` alongside it.
Add a "Go to Dashboard" CTA and today's date + upcoming deadlines to the home page.

### A4: Course and Modules PIN protection
Files: `app/dashboard/(protected)/course/page.tsx`, `app/dashboard/(protected)/modules/page.tsx`
Pattern: same as `app/dashboard/(protected)/diary/page.tsx` (reads dashboard_pin_verified cookie).
Also update SettingsClient.tsx text: "Diary, Notes and Vault" -> "Diary, Notes, Vault, Course and Modules"

### A5: Dark mode toggle in Settings
File: `app/dashboard/(protected)/settings/SettingsClient.tsx`
Add "Preferences" section. Use `useTheme` from next-themes. Pattern: `components/shared/ThemeToggle.tsx`.

### A6: Test digest button in Settings
New route: `app/api/dashboard/trigger-digest/route.ts` (auth() guard, reads CRON_SECRET, forwards to weekly-digest route).
Settings UI: "Send test now" button in a "Weekly Digest" card.

### A7: Scraper error messaging
File: `app/api/dashboard/trigger-scraper/route.ts` - map GitHub status codes to human-readable strings.
File: SettingsClient.tsx - show specific error text, not the generic hardcoded string.

---

## Group B detail - Login page

File: `app/dashboard/login/page.tsx`
- Avatar `/images/avatar.png` circular ~64px
- h1 "Isaac Adjei"
- Styled card (`border rounded-xl p-8 shadow-sm bg-card`)
- GitHub sign-in button full-width with icon
- Background `bg-background` (dark-mode aware)

---

## Group C detail - CV edits

File: `public/resume/cv.html`

1. Profile (line ~721): add "cybersecurity" after "software engineering"
2. Aston bullets (lines ~775-789): merge 2 into 1 - "Developing strong foundations in software engineering, electronics and applied mathematics while building practical project experience across embedded systems, AI/data and full-stack development"
3. Award format - BOTH same pipe style, NOT bold:
   - Aston: `Aston University, Birmingham, United Kingdom | Top 40 Finalist - Black Heritage Undergraduate of the Year Award 2026 (TargetJobs & Sky)`
   - Stanmore: `Stanmore College, London, United Kingdom | Best and Most Hardworking Student Award`
4. Skills Languages (line ~855): add Java after Python
5. Skills Web/Frameworks (line ~867): `PHP 8.2` -> `PHP`
6. Skills Cloud/DevOps (lines ~872-875): add Kubernetes after Docker
7. AstonCV project (lines ~1098-1100): `Implemented **11 security controls**` -> `Engineered **12+ security controls**`
8. Synonyms:
   - LED Cube: `Implemented four animation modes` -> `Introduced four animation modes`
   - CNC: `Implemented a safety-focused controller` -> `Engineered a safety-focused controller`
   - British Airways: `Analysed A320 maintenance schedules` -> `Reviewed A320 maintenance schedules`
   - Yunex Traffic: `Analysed intelligent transport systems` -> `Examined intelligent transport systems`
9. Experience merged bullets (tech focus):
   - Consular Intern: single bullet - "Operated end-to-end consular processing workflows using official diplomatic management systems - handling application intake, biometric data capture, document authentication and physical visa and passport production from print to issuance, supporting 100+ daily client interactions"
   - Diplomatic Admin Intern: single bullet - "Coordinated diplomatic property operations by accompanying officers on site inspections and facility surveys, managing estates documentation, maintenance schedules and office workflows to maintain mission operational standards"

---

## Group D detail - CV Word download

1. `npm install html-to-docx`
2. New file `app/api/cv-word/route.ts`: readFileSync cv.html, html-to-docx, return as .docx, optional ?template= param
3. `components/cv/CVViewer.tsx`: add "Download Word" button next to "Download PDF"
4. `.github/workflows/cv-pdf.yml`: add step to generate Isaac_Adjei_CV.docx and commit alongside PDF

---

## Group E detail - Applications + scraper

File: `app/dashboard/(protected)/applications/ApplicationsClient.tsx`
- TAB_TYPES line 155: "Summer Internships" -> "Internships"
- appBelongsToTab line 162: matching update
- APP_TYPE_OPTIONS line 188: "Summer Internship" -> "Internship"
- Default form type line 203: "Summer Internship" -> "Internship"
- Initial activeTab line 715: "Summer Internships" -> "Internships"

File: `scripts/job-scraper.py`
- Add whole-word regex `_INTERN_RE = re.compile(r'\b(intern|internship|interns)\b', re.IGNORECASE)` for "intern" matching in is_student_role
- Rename type labels: "Summer Internship" -> "Internship" in infer_type and Trackr categories
- Change insert to upsert: `supabase.table("applications").upsert(record, on_conflict="url").execute()`
- Add new sources: Indeed UK, Reed.co.uk, Milkround, Prospects.ac.uk (each as dedicated scrape_X function)

---

## Group F detail - 3-dot menus and Now section

SQL must be run first (see top of this file).

Diary (`app/dashboard/(protected)/diary/DiaryClient.tsx`):
- Replace Edit + Trash in EntryCard with MoreVertical dropdown
- Options: Edit, Hide/Show, Pin/Unpin, Lock/Unlock
- Server actions: toggleDiaryHidden, toggleDiaryPinned, toggleDiaryLocked

Notes (`app/dashboard/(protected)/notes/[folder]/NotesFolderClient.tsx`):
- ADD to existing, never remove existing functionality
- togglePin already exists - add toggleHidden and toggleLocked
- Extend existing UI with the new options

Notes page "Now" section (check app/dashboard/(protected)/notes/page.tsx):
- Add a "Now" card at top (above folder grid)
- Editable inline fields: building, studying, focused_on, listening
- Stored in config table key "now_status" as JSON
- If key missing, show placeholder prompting Isaac to fill in

Vault (check VaultClient.tsx filename):
- Add MoreVertical menu per entry: Edit, Hide/Show, Lock (global PIN required)

---

## Group G detail - CV templates

After Group C is merged:
Create 6 role-specific CV HTML files from corrected cv.html:
- `public/resume/cv-software-engineer.html`
- `public/resume/cv-embedded.html`
- `public/resume/cv-cloud.html`
- `public/resume/cv-cybersecurity.html`
- `public/resume/cv-ai-ml.html`
- `public/resume/cv-data.html`

New route: `app/cv/templates/page.tsx` - public picker page, 6 role cards.
Extend `app/api/cv-pdf/route.ts` and `app/api/cv-word/route.ts` with optional `?template=X` param.

---

## Subagent assignments

| Subagent | Groups | Run in parallel? |
|----------|--------|-----------------|
| 1 | A + B + home improvements | Yes (with 2-5) |
| 2 | C (CV edits) | Yes |
| 3 | D (Word download) | Yes |
| 4 | E (applications + scraper) | Yes |
| 5 | F (3-dot menus + Now) | Yes - but SQL must be run first |
| 6 | G (CV templates) | After Subagent 2 only |

---

## Commit rule reminder

Every commit must:
- Update `memory/LOG.md` in the same commit
- UK English, no em dashes, no Oxford commas, no AI co-author credits
- Follow: branch -> commit -> gh pr create -> gh pr merge --squash --delete-branch --auto
