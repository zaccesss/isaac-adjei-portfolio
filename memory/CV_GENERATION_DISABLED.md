# CV Generation Note

CV auto-generation has been temporarily disabled from the build script due to errors on main.

To re-enable:
- Change "build" in package.json from "next build" to "npm run generate-cvs && next build"

The generate-cvs script still exists and can be run manually with `npm run generate-cvs`.
